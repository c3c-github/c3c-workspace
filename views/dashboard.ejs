<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - C3C Workplace</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Montserrat', 'sans-serif'] },
                    colors: {
                        c3c: { blue: '#2978FF', teal: '#1DDDBD', gray: '#F2F2F2' }
                    }
                }
            }
        }
    </script>

    <style>
        body { font-family: 'Montserrat', sans-serif; background-color: #F8FAFC; }
        .bg-c3c-gradient { background: linear-gradient(135deg, #2978FF 0%, #1DDDBD 100%); }
        .nav-item.active { background-color: #EEF2FF; color: #2978FF; border-right: 3px solid #2978FF; }
        .fade-in { animation: fadeIn 0.3s ease-in-out; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }
        /* Scrollbar suave */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #f1f1f1; }
        ::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
    </style>
</head>
<body class="h-screen flex overflow-hidden">

    <!-- SIDEBAR UNIFICADO -->
    <%- include('partials/sidebar', { page: 'home' }) %>

    <!-- CONTEÚDO PRINCIPAL -->
    <main class="flex-1 flex flex-col md:ml-64 relative w-full transition-all duration-300 bg-[#F8FAFC] overflow-hidden">
        
        <!-- HEADER MOBILE -->
        <div class="md:hidden h-16 bg-white border-b border-gray-200 flex items-center justify-between px-4 shrink-0">
            <span class="font-bold text-gray-800">C3C Workplace</span>
            <button onclick="toggleMenu()" class="text-gray-600 focus:outline-none">
                <i data-lucide="menu" class="w-6 h-6"></i>
            </button>
        </div>

        <div class="flex-1 overflow-y-auto p-4 md:p-8" id="content-area">
            
            <div id="view-home" class="fade-in">
                <header class="mb-8 flex flex-col md:flex-row md:items-end justify-between gap-4">
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900">Bom dia, <%= user.nome.split(' ')[0] %>! 👋</h1>
                        <p class="text-gray-500 text-sm mt-1">Aqui está o resumo dos seus serviços hoje.</p>
                    </div>

                    <div class="flex items-center bg-white rounded-lg shadow-sm border border-gray-200 p-1">
                        <span class="px-3 text-xs font-bold text-gray-400 uppercase">Período:</span>
                        <select id="period-selector-home" onchange="onPeriodChange(this)" class="text-sm font-semibold text-gray-800 bg-transparent outline-none cursor-pointer py-1 pr-2">
                            <option>Carregando...</option>
                        </select>
                    </div>
                </header>

                <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-6 mb-8">
                    <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 flex flex-col justify-between h-32 relative overflow-hidden group">
                        <div class="absolute right-0 top-0 w-16 h-16 bg-blue-50 rounded-bl-full -mr-2 -mt-2"></div>
                        <p class="text-gray-400 text-xs font-bold uppercase tracking-wider z-10">Horas Alocadas</p>
                        <div class="flex items-end gap-1 z-10">
                            <span id="kpi-alocadas" class="text-3xl font-extrabold text-gray-800">--</span>
                            <span class="text-xs text-gray-400 font-medium mb-1.5">h</span>
                        </div>
                        <p class="text-[10px] text-gray-400 mt-1">Previsto na alocação</p>
                    </div>

                    <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 flex flex-col justify-between h-32 relative overflow-hidden">
                        <div class="absolute right-0 top-0 w-16 h-16 bg-purple-50 rounded-bl-full -mr-2 -mt-2"></div>
                        <p class="text-gray-400 text-xs font-bold uppercase tracking-wider z-10">Horas Lançadas</p>
                        <div class="flex items-end gap-1 z-10">
                            <span id="kpi-lancadas" class="text-3xl font-extrabold text-gray-800">--</span>
                            <span class="text-xs text-gray-400 font-medium mb-1.5">h</span>
                        </div>
                        <p class="text-[10px] text-gray-400 mt-1">Realizado pela equipe</p>
                    </div>

                    <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 flex flex-col justify-between h-32 relative overflow-hidden">
                        <div class="absolute right-0 top-0 w-16 h-16 bg-teal-50 rounded-bl-full -mr-2 -mt-2"></div>
                        <p class="text-gray-400 text-xs font-bold uppercase tracking-wider z-10">Eficiência</p>
                        <div class="flex items-end gap-1 z-10">
                            <span id="kpi-eficiencia" class="text-3xl font-extrabold text-[#1DDDBD]">--</span>
                            <span class="text-xs text-gray-400 font-medium mb-1.5">%</span>
                        </div>
                        <p class="text-[10px] text-gray-400 mt-1">Aprovado / Alocado</p>
                    </div>

                    <div class="bg-white p-6 rounded-xl shadow-sm border border-red-50 flex flex-col justify-between h-32 relative overflow-hidden">
                        <div class="absolute right-0 top-0 w-16 h-16 bg-red-50 rounded-bl-full -mr-2 -mt-2"></div>
                        <p class="text-red-400 text-xs font-bold uppercase tracking-wider z-10">Pendentes</p>
                        <div class="flex items-end gap-1 z-10">
                            <span id="kpi-pendentes" class="text-3xl font-extrabold text-red-600">--</span>
                            <span class="text-xs text-red-400 font-medium mb-1.5">h</span>
                        </div>
                        <p class="text-[10px] text-red-400 font-bold mt-1">Necessita Aprovação</p>
                    </div>
                </div>

                <% if (user.grupos && user.grupos.includes('GESTOR')) { %>
                <div class="bg-gradient-to-r from-[#2978FF] to-[#1DDDBD] rounded-2xl p-8 text-white flex items-center justify-between shadow-lg">
                    <div>
                        <h3 class="text-xl font-bold mb-2">Aprovações Pendentes</h3>
                        <p class="text-blue-50 text-sm max-w-md">Você tem períodos em aberto que impedem o faturamento.</p>
                        <button onclick="window.location.href='/approvals'" class="mt-6 bg-white text-blue-600 text-sm font-bold px-5 py-2.5 rounded-lg hover:bg-blue-50 transition-colors shadow-md">
                            Ir para Aprovações
                        </button>
                    </div>
                    <div class="hidden md:block opacity-90">
                        <i data-lucide="clock" class="w-24 h-24 text-white opacity-50"></i>
                    </div>
                </div>
                <% } %>
            </div>

        </div>
    </main>

    <script>
        lucide.createIcons();
        const API_URL = '/api';

        // --- FUNÇÃO MENU MOBILE ---
        function toggleMenu() {
            const sidebar = document.getElementById('sidebar');
            if (sidebar) {
                sidebar.classList.toggle('-translate-x-full');
            }
        }

        // --- INICIALIZAÇÃO E DATAS ---
        document.addEventListener('DOMContentLoaded', () => {
            loadPeriods();
        });

        async function loadPeriods() {
            try {
                const res = await fetch(`${API_URL}/periods`);
                if(res.status === 401) window.location.href = '/';
                
                const periods = await res.json();
                
                const sel = document.getElementById('period-selector-home');
                sel.innerHTML = '';
                
                if(periods.length === 0) {
                    sel.innerHTML = '<option>Sem contratos</option>';
                    return;
                }
                
                periods.forEach((p, idx) => {
                    const opt = document.createElement('option');
                    opt.value = JSON.stringify({ start: p.DataInicio__c, end: p.DataFim__c });
                    opt.text = p.Name;
                    if(idx === 0) opt.selected = true;
                    sel.appendChild(opt);
                });

                if (sel.options.length > 0) {
                    triggerDataLoad(sel.value);
                }

            } catch (err) { console.error(err); }
        }

        function onPeriodChange(changedElement) {
            triggerDataLoad(changedElement.value);
        }

        function triggerDataLoad(jsonDates) {
            if(!jsonDates) return;
            const dates = JSON.parse(jsonDates);
            loadDashboardKPIs(dates.start, dates.end);
        }

        // --- CARGA DE KPIS (4 CARDS) ---
        async function loadDashboardKPIs(start, end) {
            const ids = ['kpi-alocadas', 'kpi-lancadas', 'kpi-eficiencia', 'kpi-pendentes'];
            const els = ids.map(id => document.getElementById(id));
            els.forEach(el => el.style.opacity = '0.5');

            try {
                const res = await fetch(`${API_URL}/dashboard/metrics?inicio=${start}&fim=${end}`);
                if (res.ok) {
                    const data = await res.json();
                    
                    els[0].innerText = data.totalAlocadas.toFixed(0);
                    els[1].innerText = data.totalLancadas.toFixed(1);
                    els[2].innerText = data.eficiencia;
                    els[3].innerText = data.totalPendentes.toFixed(1);

                    // Cor Eficiência
                    const elEfic = els[2];
                    if(data.eficiencia > 100) elEfic.className = "text-3xl font-extrabold text-red-500";
                    else if(data.eficiencia >= 95) elEfic.className = "text-3xl font-extrabold text-[#1DDDBD]";
                    else elEfic.className = "text-3xl font-extrabold text-yellow-500";

                } else {
                    els.forEach(el => el.innerText = "-");
                }
            } catch (err) { console.error(err); } 
            finally { els.forEach(el => el.style.opacity = '1'); }
        }
    </script>
</body>
</html>